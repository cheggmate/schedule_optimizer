Readme.txt

instructions to open file:

import root folder (project) into directory in eclipse IDE
open Java file.

export project into readfile.jar


run project:
javac readfile.java
jar cfm readfile.jar manifest.mf readfile.class
java -jar readfile.jar